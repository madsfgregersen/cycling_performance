# Historical backfill

A one-time bulk import of historical `.fit` files is needed to seed CTL
history (42+ days minimum). This is time-sensitive given race proximity
and should be executed early. It feeds Buckets 1 and 2 (see
`architecture.md`).
